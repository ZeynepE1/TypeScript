function greeter(name) {
  console.log("hello " + name);
}

let user = "zeynep";
greeter(user);
// --tsc greeter.ts-- terminale yaz js dosyası oluşur.
// --node greeter.js-- terminale yaz console ifadesini gör.
